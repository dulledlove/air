worker: python3 
import os

def file_exists(main.py, /storage/emulated/0/notifer = os.getcwd()):

	"""	Check if the specified file exists at the specified directory

	"""

	files = os.listdir(path)

	return filename in files 
